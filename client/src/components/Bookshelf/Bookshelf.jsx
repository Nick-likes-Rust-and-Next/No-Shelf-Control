import { Box } from "@mui/material";
import BookList from "../BookList/BookList";

const Bookshelf = () => {
    return (
        <Box>
            <BookList />
        </Box>
    );
};

export default Bookshelf;
